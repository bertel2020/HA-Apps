# Direkte Laufzeitabhängigkeiten. Nach Änderungen neu auflösen mit:
# uv pip compile requirements.in --python-version 3.12 --python-platform linux -o requirements.txt
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pyarrow>=16.0.0
jinja2>=3.1.0
python-multipart>=0.0.9
